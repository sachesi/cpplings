// Booleans (`bool`)

#include <iostream>

int main() {
    bool is_morning = true;
    if (is_morning) {
        std::cout << "Good morning!\n";
    }

    // TODO: Define a boolean variable with the name `is_evening` before the `if`
    // statement below. The value of the variable should be the negation
    // (opposite) of `is_morning`.

    if (is_evening) {
        std::cout << "Good evening!\n";
    }
}
